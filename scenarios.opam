synopsis:     "scenarios"
description:  "scenarios"
name:         "scenarios"
opam-version: "2.0"
maintainer:   "fibletype@gmail.com"
authors:      "fibletype"

depends: [
  "coq"           { >= "8.11.0" }
  "dune"          { >= "2.8.0"  }
]


build: [ "dune" "build" "-p" name "-j" jobs ]
